const multiplicarPorDois = (x) => {
    return x * 2;
}

console.log(multiplicarPorDois(2));

const multiplicarPorTres = x => x * 3; // Posso omitir os parênteses e o return nesse tipo de arrow  function se só houver um parâmetro.

console.log(multiplicarPorTres(3));